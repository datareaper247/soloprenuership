---
description: Auto-learned patterns from session analysis
---

# Auto-Learned Patterns

*Last updated: Fri Mar 20 20:41:30 CET 2026*

## Verification Commands Used
These commands were used to verify work - continue using them:

```
cat /Users/fsd/Projects/taptap/src/screens/profile/SettingsScreen.tsx 2>/dev/null | head
cat /Users/fsd/Projects/taptap/src/screens/subscription/PaywallScreen.tsx | head
cat /Users/fsd/Projects/taptap/src/screens/subscription/PaywallScreen.tsx | wc -l && grep -n \"isAuthenticated\\|handleSubscribe\\|disabled\\|anon\\|sign\" /Users/fsd/Projects/taptap/src/screens/subscription/PaywallScreen.tsx | head
cat <<'EOF'\nfix(app-store): resolve P0/P1 blockers from App Store readiness audit\n\nP0 Fixes (code-addressable):\n- UX-P0-2: Add first-launch 'no controls' explanation overlay in TutorialPlayerScreen\n- UX-P0-3: Fix Settings sign-out to clear auth + subscription state (was missing clearAuth/reset)\n- NET-P0-1: Add 10s timeout + offline error UI to PaywallScreen (was hanging indefinitely)\n- NET-P0-2: Wire syncAll() on network reconnect in AuthProvider\n\nP1 Fixes:\n- SEC-H2: Remove localhost:3000 hardcode from config (empty string fallback)\n- SEC-H3: Migrate subscription store from AsyncStorage to SecureStore\n- SUB-001: Add \"/month\" billing period to Paywall price display\n- SUB-002: Move Restore Purchases button to head
catch {\n              Alert.alert('Something Went Wrong', 'Could not clear cache. Please try again.');\n            }\n          },\n        },\n      ]\n    );\n  }, [trigger]);\n\n  const handleContactSupport = useCallback(() => {\n    Alert.alert(\n      'Contact Support',\n      'How would you like to reach us?',\n      [\n        { text: 'Email Us', onPress: () => Linking.openURL('mailto:support@taptap.app?subject=TapTap%20Support%20Request') },\n        { text: 'Cancel', style: 'cancel' },\n      ]\n    );\n  }, []);\n\n  const handleShowPrivacyPolicy = useCallback(async () => {\n    await WebBrowser.openBrowserAsync('https://taptap.app/privacy');\n  }, []);\n\n  const handleShowTerms = useCallback(async () => {\n    await WebBrowser.openBrowserAsync('https://taptap.app/terms');\n  }, []);\n\n  const handleEditDisplayName = useCallback(() => {\n    Alert.prompt(\n      'Edit Display Name',\n      'Enter your name:',\n      [\n        { text: 'Cancel', style: 'cancel' },\n        {\n          text: 'Save',\n          onPress: (name?: string) => {\n            if (name && name.trim()) {\n              updateUser({ displayName: name.trim() });\n            }\n          },\n        },\n      ],\n      'plain-text',\n      user?.displayName ?? ''\n    );\n  }, [user?.displayName, updateUser]);\n\n  const SettingRow = ({\n    icon,\n    title,\n    description,\n    value,\n    onToggle,\n  }: {\n    icon: keyof typeof Ionicons.glyphMap;\n    title: string;\n    description: string;\n    value: boolean;\n    onToggle: () => void;\n  }) => (\n    <View style={styles.settingRow}>\n      <View style={styles.settingIcon}>\n        <Ionicons name={icon} size={24} color={colors.primary[500]} />\n      </View>\n      <View style={styles.settingContent}>\n        <Text variant=\"body\" weight=\"semibold\">\n          {title}\n        </Text>\n        <Text variant=\"caption\" color=\"secondary\">\n          {description}\n        </Text>\n      </View>\n      <Switch\n        value={value}\n        onValueChange={onToggle}\n        trackColor={{ false: colors.neutral[300], true: colors.primary[300] }}\n        thumbColor={value ? colors.primary[500] : colors.neutral[100]}\n        ios_backgroundColor={colors.neutral[300]}\n        accessibilityLabel={`${title}: ${value ? 'On' : 'Off'}`}\n        accessibilityRole=\"switch\"\n        style={{ transform: [{ scale: 1.2 }] }}\n      />\n    </View>\n  );\n\n  const ActionRow = ({\n    icon,\n    title,\n    description,\n    onPress,\n    showChevron = true,\n    iconColor,\n  }: {\n    icon: keyof typeof Ionicons.glyphMap;\n    title: string;\n    description?: string;\n    onPress?: () => void;\n    showChevron?: boolean;\n    iconColor?: string;\n  }) => {\n    const rowContent = (\n      <>\n        <View style={styles.settingIcon}>\n          <Ionicons name={icon} size={24} color={iconColor || colors.primary[500]} />\n        </View>\n        <View style={styles.settingContent}>\n          <Text variant=\"body\" weight=\"semibold\">\n            {title}\n          </Text>\n          {description && (\n            <Text variant=\"caption\" color=\"secondary\">\n              {description}\n            </Text>\n          )}\n        </View>\n        {showChevron && (\n          <Ionicons name=\"chevron-forward\" size={24} color={colors.neutral[400]} />\n        )}\n      </>\n    );\n\n    if (!onPress) {\n      return (\n        <View style={styles.settingRow}>\n          {rowContent}\n        </View>\n      );\n    }\n\n    return (\n      <TouchableOpacity\n        activeOpacity={0.7}\n        style={styles.settingRow}\n        onPress={() => {\n          trigger('light');\n          onPress();\n        }}\n        accessibilityRole=\"button\"\n        accessibilityLabel={title}\n        accessibilityHint={description}\n      >\n        {rowContent}\n      </TouchableOpacity>\n    );\n  };\n\n  return (\n    <SafeAreaView style={[styles.container, { backgroundColor: accessibleColors.background }]}>\n      {/* Header */}\n      <View style={[styles.header, { backgroundColor: accessibleColors.background }]}>\n        <TouchableOpacity\n          activeOpacity={0.7}\n          onPress={() => {\n            trigger('light');\n            navigation.goBack();\n          }}\n          style={styles.backButton}\n          accessibilityLabel=\"Go back\"\n          accessibilityRole=\"button\"\n        >\n          <Ionicons name=\"arrow-back\" size={28} color={accessibleColors.text} />\n        </TouchableOpacity>\n        <Text variant=\"h2\">Settings</Text>\n        <View style={styles.headerSpacer} />\n      </View>\n\n      <ScrollView\n        testID=\"screen-settings\"\n        style={styles.scrollView}\n        contentContainerStyle={styles.content}\n        showsVerticalScrollIndicator={false}\n      >\n        {/* Account Section */}\n        <Card variant=\"outlined\" style={styles.section}>\n          <Text variant=\"h3\" style={styles.sectionTitle}>\n            Account\n          </Text>\n\n          <ActionRow\n            icon=\"person-circle\"\n            title=\"Display Name\"\n            description={user?.displayName ?? 'Not set'}\n            onPress={handleEditDisplayName}\n            showChevron={true}\n          />\n\n          <ActionRow\n            icon=\"mail\"\n            title=\"Email\"\n            description={user?.email || 'Not signed in'}\n            showChevron={false}\n          />\n        </Card>\n\n        {/* Notifications Section */}\n        <Card variant=\"outlined\" style={styles.section}>\n          <Text variant=\"h3\" style={styles.sectionTitle}>\n            Notifications\n          </Text>\n\n          <SettingRow\n            icon=\"notifications\"\n            title=\"Push Notifications\"\n            description=\"Get notified about new tutorials\"\n            value={appPreferences.notificationsEnabled}\n            onToggle={() => {\n              trigger('selection');\n              updateAppPreferences({ notificationsEnabled: !appPreferences.notificationsEnabled });\n            }}\n          />\n\n          <SettingRow\n            icon=\"alarm\"\n            title=\"Daily Reminders\"\n            description=\"Remind me to practice daily\"\n            value={appPreferences.dailyRemindersEnabled}\n            onToggle={() => {\n              trigger('selection');\n              updateAppPreferences({ dailyRemindersEnabled: !appPreferences.dailyRemindersEnabled });\n            }}\n          />\n        </Card>\n\n        {/* App Preferences Section */}\n        <Card variant=\"outlined\" style={styles.section}>\n          <Text variant=\"h3\" style={styles.sectionTitle}>\n            App Preferences\n          </Text>\n\n          <ActionRow\n            icon=\"language\"\n            title=\"Language\"\n            description=\"English\"\n            showChevron={false}\n          />\n\n          <SettingRow\n            icon=\"play\"\n            title=\"Auto-play Videos\"\n            description=\"Automatically start the next tutorial\"\n            value={appPreferences.autoPlayEnabled}\n            onToggle={() => {\n              trigger('selection');\n              updateAppPreferences({ autoPlayEnabled: !appPreferences.autoPlayEnabled });\n            }}\n          />\n\n          <SettingRow\n            icon=\"wifi\"\n            title=\"Use WiFi for Videos\"\n            description=\"Don't use phone data for downloading videos\"\n            value={appPreferences.wifiOnlyDownload}\n            onToggle={() => {\n              trigger('selection');\n              updateAppPreferences({ wifiOnlyDownload: !appPreferences.wifiOnlyDownload });\n            }}\n          />\n        </Card>\n\n        {/* Data & Storage Section */}\n        <Card variant=\"outlined\" style={styles.section}>\n          <Text variant=\"h3\" style={styles.sectionTitle}>\n            Data & Storage\n          </Text>\n\n          <ActionRow\n            icon=\"trash\"\n            title=\"Free Up Space\"\n            description=\"Remove files you no longer need\"\n            onPress={handleClearCache}\n          />\n\n          <ActionRow\n            icon=\"stats-chart\"\n            title=\"App Storage\"\n            description=\"Videos are included in the app\"\n            showChevron={false}\n          />\n        </Card>\n\n        {/* About Section */}\n        <Card variant=\"outlined\" style={styles.section}>\n          <Text variant=\"h3\" style={styles.sectionTitle}>\n            About\n          </Text>\n\n          <ActionRow\n            icon=\"information-circle\"\n            title=\"App Version\"\n            description=\"1.0.0 (Build 1)\"\n            showChevron={false}\n          />\n\n          <ActionRow\n            icon=\"shield-checkmark\"\n            title=\"Privacy Policy\"\n            description=\"How we protect your data\"\n            onPress={handleShowPrivacyPolicy}\n          />\n\n          <ActionRow\n            icon=\"document-text\"\n            title=\"Terms of Service\"\n            description=\"Read our terms\"\n            onPress={handleShowTerms}\n          />\n\n          <ActionRow\n            icon=\"help-circle\"\n            title=\"Contact Support\"\n            description=\"We're here to help\"\n            onPress={handleContactSupport}\n          />\n\n          <Text\n            variant=\"caption\"\n            style={{ color: colors.text.secondary, textAlign: 'center', marginTop: 8 }}\n            accessibilityElementsHidden\n            importantForAccessibility=\"no\"\n          >\n            {'Not affiliated with or endorsed by Apple Inc.\\niOS app names shown in tutorials are trademarks of Apple Inc.'}\n          </Text>\n        </Card>\n\n        {/* Sign Out Button */}\n        <View style={styles.signOutContainer}>\n          <Button\n            title=\"Sign Out\"\n            variant=\"outline\"\n            size=\"large\"\n            fullWidth\n            onPress={handleSignOut}\n            accessibilityHint=\"Sign out of your account\"\n            testID=\"button-sign-out\"\n          />\n        </View>\n\n        {/* Delete Account */}\n        <View style={styles.deleteAccountContainer}>\n          <Button\n            title=\"Delete Account\"\n            variant=\"danger\"\n            size=\"large\"\n            fullWidth\n            onPress={handleDeleteAccount}\n            accessibilityHint=\"Permanently delete your account and all data\"\n            testID=\"button-delete-account\"\n          />\n        </View>\n\n        {/* Footer spacing */}\n        <View style={{ height: spacing[8] }} />\n      </ScrollView>\n    </SafeAreaView>\n  );\n}\n\nconst styles = StyleSheet.create({\n  container: {\n    flex: 1,\n  },\n  header: {\n    flexDirection: 'row',\n    alignItems: 'center',\n    justifyContent: 'space-between',\n    paddingHorizontal: spacing[5],\n    paddingTop: Platform.OS === 'ios' ? spacing[2] : spacing[5],\n    paddingBottom: spacing[4],\n    borderBottomWidth: 1,\n    borderBottomColor: colors.neutral[200],\n  },\n  backButton: {\n    minWidth: touchTargets.small,\n    minHeight: touchTargets.small,\n    justifyContent: 'center',\n    alignItems: 'center',\n  },\n  head
```

## Issues Encountered
These issues were detected - watch for them:

```
error
eRroR
Error
```

## Recommendations
- Always verify file paths before writing
- Run tests after code changes
- Check imports exist before using them
